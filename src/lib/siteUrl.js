/** Canonical production host for SPIKE (1CMA portal). */
export const PRODUCTION_HOST = 'portal.1cma.online';

/** Full production origin (no trailing slash). */
export const PRODUCTION_ORIGIN = `https://${PRODUCTION_HOST}`;

/** Cloudflare Pages default hostname (staging / fallback). */
export const PAGES_STAGING_HOST = 'spike-asc.pages.dev';

export const PAGES_STAGING_ORIGIN = `https://${PAGES_STAGING_HOST}`;
