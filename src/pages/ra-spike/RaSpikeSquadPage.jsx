import { PageContainer } from '../../components/layout/PageContainer.jsx';

/** RA-SPIKE squad — Phase 0 placeholder. */
export function RaSpikeSquadPage() {
  return (
    <PageContainer>
      <section className="spike-card mx-auto max-w-2xl">
        <h1 className="text-2xl font-bold text-slate-900">Squad</h1>
        <p className="mt-2 text-slate-600">
          Squad roster, weekly objectives, and leaderboard arrive in Phase 6.
        </p>
      </section>
    </PageContainer>
  );
}
