import { PageContainer } from '../../components/layout/PageContainer.jsx';

/** RA-SPIKE playbook — Phase 0 placeholder. */
export function RaSpikePlaybookPage() {
  return (
    <PageContainer>
      <section className="spike-card mx-auto max-w-2xl">
        <h1 className="text-2xl font-bold text-slate-900">Playbook</h1>
        <p className="mt-2 text-slate-600">
          Weekly learning cards (Learn · Workshop · Assignment · Reflection · Submit) arrive in Phase 4.
        </p>
      </section>
    </PageContainer>
  );
}
