# RA-SPIKE local dev — copy to .env.ra-spike.local (gitignored) or set in .env.
# Loaded by Vite when using --mode ra-spike (npm run dev:ra-spike).
VITE_RA_SPIKE_SUPABASE_URL=
VITE_RA_SPIKE_SUPABASE_ANON_KEY=
