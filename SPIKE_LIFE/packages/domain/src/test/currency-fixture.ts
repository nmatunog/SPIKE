import type { CurrencyConfig } from '@spike-life/content-core'

/** Test and bootstrap default — mirrors Philippines MVP pack currency shape. */
export const TEST_CURRENCY: CurrencyConfig = {
  code: 'PHP',
  locale: 'en-PH',
  maximumFractionDigits: 0,
}
